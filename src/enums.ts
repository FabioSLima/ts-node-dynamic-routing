export enum RouteMethod {
    GET = "GET",
    POST = "POST",
    PUT = "PUT",
    DELETE = "DELETE",
    PATCH = "PATCH",
    OPTIONS = "OPTIONS",
}

export enum RouteConfigType {
    CODE,
    /* TODO: Import routes from file
    FILE,
    */
}

export enum RouteExportType {
    EXPRESS,
}